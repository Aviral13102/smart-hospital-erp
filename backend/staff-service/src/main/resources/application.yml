server:
  port: 8085

spring:
  application:
    name: staff-service
  
  datasource:
    url: jdbc:postgresql://localhost:5432/hospital_erp
    username: admin
    password: password
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

logging:
  level:
    com.smarthospital.staff: DEBUG
    org.hibernate.SQL: DEBUG
